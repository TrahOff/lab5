from MyLib.files import folder, file, textfile, csvfile
from MyLib.data import csvdata, txtdata


class Const:
    fold = folder.Folder()
    file = file.File()
    textfile = textfile.TextFile()
    csvfile = csvfile.CSVFile()
    csvData = csvdata.CSVData()
    txtData = txtdata.TXTData()
